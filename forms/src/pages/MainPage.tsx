// import { useSelector } from "react-redux";
// import { FormFields } from "../types/formTypes";

export default function MainPage() {
  // const formData: FormFields = useSelector((state: any) => state.uncontrolledComponent)
  return (
    <>
      <h1>Main Page</h1>
      {/* <p>
        {formData.name}
        {formData.age}
      </p> */}
    </>
  );
}
